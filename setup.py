from setuptools import setup, find_packages

setup(
    name = "src",
    version= "0.0.1",
    description = "its BigMart Sales Prediction package",
    autthor = "Developer Ashish",
    packages = find_packages()
)